# Report evidence

No rendered result sheet, result register, or report card has yet been safely captured. Capture must use the local VS Code instance at `http://127.0.0.1:8000`, identify `db/school_management.sql` as the MariaDB source, record whether MSSQL was reachable, and must not be labelled as remote/deployed evidence. Result generation was not invoked.

Future evidence must be taken from the isolated local Phase 0 database or supplied as sanitized existing output. Remove names, GR numbers, contact details, government identifiers, and other student/user PII before committing evidence.
