# Screenshot evidence

## Supersession notice — 2026-08-18

The remote production-testing system is separate and not connected to this environment. `DEPLOY-UNKNOWN-localhost-login-404.png` is retained as historical prior-run evidence, but the request to `http://localhost/login` was outside the corrected Phase 0 scope. It proves only that the default local IIS root answered that request; it is neither remote deployment evidence nor local Laravel UI evidence.

Required screenshots remain `PENDING LOCAL CAPTURE` from `http://127.0.0.1:8000` using the provenance required by `LOCAL_VSCODE_RUN_GUIDE.md`. Do not infer application behavior from the IIS error image.
